/* tslint:disable */
import { Injectable } from '@angular/core';
import { Path } from 'src/app/_helpers/path';

/**
 * Global configuration for {{prefix}} services
 */
@Injectable({
  providedIn: 'root',
})
export class {{configurationClass}} {
  rootUrl: string = '{{{rootUrl}}}';
  constructor(private path: Path) {
    this.rootUrl = path.backendUrl
  }
}

export interface {{configurationInterface}} {
  rootUrl?: string;
}
